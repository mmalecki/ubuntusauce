#!/usr/bin/env bash

sudo apt-get update -y

# install packages I use
sudo apt-get install -y whois dnsutils zsh strace clang tcpdump ghc mosh nmap exuberant-ctags rsync cmake bzr parallel mercurial gdb zip unzip docker imagemagick golang-go pv sysstat subversion units socat ocaml software-properties-common apt-src ack-grep python-pip python-dev libssl-dev automake erlang
